{
  "name": "xallor-remote-mcp",
  "version": "0.1.1",
  "description": "XallorRemote：MCP 头 + 本机 Runtime（安装一个包即可 ensure / 被控）",
  "type": "module",
  "bin": {
    "xallor-remote-mcp": "dist/index.js",
    "xallor-remote": "dist/runtime-bin.js"
  },
  "files": [
    "dist",
    "vendor"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "test": "tsc && node --test dist/version.test.js dist/ensure.test.js",
    "pack:release": "node ../../scripts/pack-mcp-with-runtime.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.8.0"
  }
}
