---
description: 先定模块再写代码；禁止把新功能继续塞进已有大文件
alwaysApply: true
---

# 模块先行（禁止巨石文件）

改代码前先定 **谁拥有这件事**（现有目录或新建目录），再落盘。禁止为了少开文件，把第二种职责追加进已经偏大的生产文件。事后再拆成本远高于当时分开。

建议落点见 [`docs/stack.md`](docs/stack.md) §3：`internal/ipc`、`internal/runtime`、`internal/relay`、`internal/protocol`、`packages/mcp`、`apps/gui`。

## 落盘前自问

1. 这件事属于哪个模块？对不上就 **新建目录**，不要开在 `cmd/` 入口或 App 壳里。
2. 目标文件是否已经在做另一件事？是则 **新文件**，只从壳层 import。
3. 改完后该文件是否仍是单一职责？否就当场拆，不要「先塞进去以后再拆」。

## 体量（生产源文件，不含 `*_test.go` / `*.test.*` / 生成物）

| | 标准 |
|--|------|
| 新文件 | 逻辑文件目标 **≤300 行**；不要开篇就写 500+ |
| 已有文件 **>400 行** | **禁止**再往里加新功能/新分支；抽模块或新文件 |
| 已有文件 300–400 行 | 只修原职责；新能力另文件 |
| 可接受的大文件 | **纯表**：错误码表、fixture、生成 JSON |

「行数」是刹车，不是目标。正确拆法是 **按职责**（types / 纯函数 / IPC / 策略 / Popen），不是把 8 行 helper 拆成一堆空文件。

## 禁止

- 在 `main.go` / 单一 `runtime.go` 上按功能继续堆 `if`
- 把 storage / 网络 / 策略 / UI 写进同一个文件
- MCP 包里实现 WSS 或策略
- GUI 里实现 Relay 协议
- 复制同一套 clamp / 哈希 / 路径规范化；共用的放已有模块

## 允许

- 薄 barrel（`doc.go` / `index.ts` 只 re-export）
- 测试文件按场景拆；单测可以比实现长
- 在 **新模块目录内** 用多个短文件，而不是一个 800 行的 `foo.go`
